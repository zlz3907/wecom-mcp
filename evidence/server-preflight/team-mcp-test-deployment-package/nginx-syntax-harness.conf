# Parser-only harness: the sites-available template is included by nginx.conf's
# http block in production. Do not install this harness on the server.
events {}
http {
    include nginx-mcp.jyiai.com-gmzoop.conf;
}
