# 企业微信智能表格字段兼容性矩阵

## 判定口径

- **已验证可读写**：在已登记的“字段编码验证表”中读到人工填写的原始值，使用该值新增记录并更新后回读成功。
- **系统只读**：新增记录时由企业微信生成；可读取，但 MCP 不应提交写入值。
- **读取异常**：UI 显示有值，但当前受管 `get_records` 原始回包未提供可用值；不影响已确认的官方写入契约。
- **未验证**：尚未建立字段或没有可用于测试的值。

本矩阵只说明企业微信当前接口与 MCP 的字段能力，不等同于任意业务表已经存在这些字段，也不授权业务数据写入。

## 已验证可读写

| UI 字段 | 企业微信字段类型 | 验证结果 | 已知写入形态 |
| --- | --- | --- | --- |
| 文本 | `FIELD_TYPE_TEXT` | 读、插入、更新、回读成功 | 文本单元格数组 |
| 单选 | `FIELD_TYPE_SINGLE_SELECT` | 读、插入、更新、回读成功 | 选项对象数组：`[{"text":"已登记选项名"}]`。本地 Schema 先校验选项名，避免企业微信创建未知选项。 |
| 多选 | `FIELD_TYPE_SELECT` | 读、插入、回读成功 | 选项对象数组（含选项 ID） |
| 数字 | `FIELD_TYPE_NUMBER` | 读、插入、回读成功 | 数字 |
| 百分数 | `FIELD_TYPE_PERCENTAGE` | 读、插入、回读成功 | 数字 |
| 货币 | `FIELD_TYPE_CURRENCY` | 读、插入、回读成功 | 数字 |
| 进度 | `FIELD_TYPE_PROGRESS` | 读、插入、回读成功 | 数字 |
| 日期 | `FIELD_TYPE_DATE_TIME` | 读、插入、回读成功 | 毫秒时间戳字符串 |
| 人员 | `FIELD_TYPE_USER` | 读、插入、回读成功 | 人员对象数组 |
| 群聊 | `FIELD_TYPE_WWGROUP` | 读、插入、回读成功 | 群对象数组 |
| 图片 | `FIELD_TYPE_IMAGE` | 读、插入、回读成功 | 图片对象数组 |
| 链接 | `FIELD_TYPE_URL` | 读、插入、回读成功 | 链接单元格数组 |
| 电话 | `FIELD_TYPE_PHONE_NUMBER` | 读、插入、回读成功 | 字符串 |
| 邮箱 | `FIELD_TYPE_EMAIL` | 读、插入、回读成功 | 字符串 |
| 地理位置 | `FIELD_TYPE_LOCATION` | 读、插入、回读成功 | 位置对象数组 |
| 复选框 | `FIELD_TYPE_CHECKBOX` | 读、插入、更新、回读成功 | 布尔值 |
| 条码 | `FIELD_TYPE_BARCODE` | 读、插入、回读成功 | 字符串 |
| 关联 | `FIELD_TYPE_REFERENCE` | 跨表配置、读取、新增、更新和回读成功 | 调用方传入受控对象数组 `[{"record_id":"RECORD_ID"}]`；MCP 校验目标子表和真实记录 ID 后，发送企业微信时编译为字符串数组 `["RECORD_ID"]`。Zoop 只使用跨表关联。 |

## 问题、限制与待验证项

| UI 字段 | 企业微信字段类型 | 当前状态 | 事实与处理原则 |
| --- | --- | --- | --- |
| 文件 | `FIELD_TYPE_ATTACHMENT` | 可读；当前公开“添加记录”契约未提供写入形态 | 已读到真实文件对象；将其原样新增时企业微信返回 `2020036`。2026-08-09 复核官方“添加记录”的完整 Value 字段清单：其中没有 `FIELD_TYPE_ATTACHMENT`，因此不能把临时素材、微盘上传结果或读取对象猜测为附件单元格写入值。当前 MCP 明确拒绝写入；如需支持，必须先取得企业微信针对智能表格附件列的正式上传与写入契约，再单独验证。 |
| 自动编号 | `FIELD_TYPE_AUTONUMBER` | 系统只读 | 新增记录后正常返回编号对象；不接受 MCP 写入。 |
| 创建人 | `FIELD_TYPE_CREATED_USER` | 系统只读 | 新增记录后可读；不接受 MCP 写入。 |
| 最后编辑人 | `FIELD_TYPE_MODIFIED_USER` | 系统只读 | 更新记录后可读；不接受 MCP 写入。 |
| 创建时间 | `FIELD_TYPE_CREATED_TIME` | 系统只读 | 新增记录后可读；不接受 MCP 写入。 |
| 最后编辑时间 | `FIELD_TYPE_MODIFIED_TIME` | 系统只读 | 更新记录后可读；不接受 MCP 写入。 |
| 公式 | `FIELD_TYPE_FORMULA` | 未验证 | 当前字段验证表及旧 MCP 接口清单未覆盖该类型；不得推断可写。 |
| 同表自关联 | `FIELD_TYPE_REFERENCE` | 读取未验证；不在 Zoop 使用范围 | 字段目标为当前子表时，UI 显示的关联值曾在官方 `get_records` 原始回包中返回 `[]`。Zoop 原则上不关联本表记录，因此不继续投入排查，也不得把该现象扩大为跨表关联不可用。 |
| 查找引用 | `FIELD_TYPE_LOOKUP` | 未验证 | 依赖关联字段；当前不纳入自动写入能力。 |
| AI 字段 | 未确认公开 API 类型 | 未验证 | 截图中的功能入口不等同于公开智能表字段类型；需以线上字段定义为准。 |

## 固化规则

1. 常规 `wecom_record_apply` 仅允许写入“已验证可读写”的字段类型。
2. 复杂值必须使用验证表回读确认过的对象形态与内部 ID；不得根据 UI 文本猜测。
3. 系统只读、文件、公式和查找引用一律拒绝自动写入，直到取得可验证的接口契约和回读证据。文件字段目前不是“尚未尝试”，而是官方“添加记录”字段清单未列出 `FIELD_TYPE_ATTACHMENT`；不得用通用临时素材或微盘文件接口跨类型猜测写入。
4. 关联字段仅允许跨表使用：本地 Schema 中 `property_reference.sub_id` 必须指向明确的目标子表，写入值必须使用该子表真实存在的记录 ID，并保留接口成功回执和回读结果。同表自关联不属于 Zoop 能力范围。
5. 不以 `errcode=0` 单独判定关联成功。当前租户对对象数组 wire value 会返回成功但不落库；只有字符串数组写入后回读到目标 `record_id` 才算通过。

## 关联字段的官方契约

企业微信“添加记录”接口将关联字段定义为 `FIELD_TYPE_REFERENCE`。写入值必须指向被关联表中实际存在的记录 ID。官方契约列出对象数组和简化字符串数组两种形式，但当前租户的真实验证结论是：对象数组返回成功却不落库，字符串数组可新增、更新并回读。因此 MCP 对外保留结构化对象输入，发送企业微信时统一编译为字符串数组：

```json
{
  "关联字段ID": ["RECORD_ID"]
}
```

当前结论同时来自官方接口说明和已登记验证表的真实测试：

- 2026-08-09，同表自关联的新增、更新获得成功回执，但官方 `get_records` 回读值为 `[]`。
- 2026-08-10，将同一关联字段改为跨表关联后，`get_fields` 返回 `property_reference.sub_id=t2mRWJ`；官方 `get_records` 在两条来源记录中均返回 `["rMQ9ic"]`，再次查询目标子表可读取目标记录 `rMQ9ic`，其主文本为 `a1`。
- 2026-08-10，对象数组在字段验证表和 Zoop 正式表均出现 `errcode=0` 但回读 `[]`、UI 为空；不能视为成功。
- 2026-08-10，改为字符串数组后，字段验证表完成跨表目标新增、来源新增、来源更新和回读；随后 S01 2 条、S03 5 条、S06 2 条正式关联全部 `readback_verified=true`。
- 因此，跨表关联的“字段配置 → 目标子表真实 record_id → 字符串数组写入 → 回读目标 record_id → 目标记录内容”闭环已经成立。Zoop 只使用跨表关联；同表自关联不再作为主流程阻塞项。

来源：[企业微信开发者文档：添加记录](https://developer.work.weixin.qq.com/document/path/101240)。

## 文件字段结论

企业微信“添加记录”接口当前列出的可写字段包含文本、数字、复选框、日期、图片、成员、链接、多选、进度、电话、邮箱、单选、地理位置、货币、百分数、条码和关联；该官方清单不包含 `FIELD_TYPE_ATTACHMENT`。因此本 MCP 将附件列固化为“可读、不可写”的受控能力，而不是把其他企业微信文件上传接口的返回值擅自拼装到智能表格记录中。

来源：[企业微信开发者文档：添加记录](https://developer.work.weixin.qq.com/document/path/101240)。
